div.grid
  - md_erb_files.each do |file|
    - file_html, file_metadata = process_md_erb(file)
    div.grid-item
      - if file_metadata.to_h.blank?
        - file_metadata.tags = ["unknown"]
      div.metadata data-metadata="" = file_metadata.to_h.to_json
      div.content
        == file_html
